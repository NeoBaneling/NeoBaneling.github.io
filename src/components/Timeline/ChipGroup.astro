---
interface Props {
	chips?: string[];
}

const { chips } = Astro.props;

if (!chips) return;
---

<div class="mt-2 flex flex-row flex-wrap gap-2">
	{chips.map((chip) => <div class="chip">{chip}</div>)}
</div>
